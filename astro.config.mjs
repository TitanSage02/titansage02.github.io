// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://titansage02.github.io',
  // Pas de base nécessaire car c'est le site utilisateur principal
});
