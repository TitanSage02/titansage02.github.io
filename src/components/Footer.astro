---
const currentYear = new Date().getFullYear();
---

<footer class="main-footer">
  <div class="container footer-content">
    <div class="footer-section about">
      <h3>Espérance AYIWAHOUN</h3>
      <p>Passionné par l'IA, la Robotique et l'Innovation</p>
    </div>
    
    <div class="footer-section contact">
      <h4>Contact & Social</h4>
      <div class="social-links">
        <a href="https://github.com/TitanSage02" target="_blank" rel="noopener">
          GitHub
        </a>
        <a href="https://linkedin.com/in/esperance-ayiwahoun" target="_blank" rel="noopener">
          LinkedIn
        </a>
        <a href="mailto:eayiwahoun@gmail.com">
          eayiwahoun@gmail.com
        </a>
      </div>
    </div>
  </div>
  
  <div class="footer-bottom">
    <div class="container">
      <p>© {currentYear} Espérance AYIWAHOUN. Tous droits réservés.</p>
    </div>
  </div>
</footer>

<style>
  .main-footer {
    background: var(--color-bg-secondary);
    color: var(--color-text);
    padding: var(--space-md) 0;
    margin-top: var(--space-lg);
    border-top: 1px solid var(--color-border);
  }
  
  .footer-content {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-lg);
    margin-bottom: var(--space-lg);
  }
  
  .footer-section.about h3 {
    color: var(--color-primary);
    margin-bottom: var(--space-sm);
    font-size: 1.2rem;
  }
  
  .footer-section.contact h4 {
    color: var(--color-primary);
    margin-bottom: var(--space-sm);
    font-size: 1rem;
    font-weight: 600;
  }
  
  .footer-section p {
    color: var(--color-text-light);
    margin-bottom: var(--space-sm);
    line-height: 1.6;
  }
  
  .social-links {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }
  
  .social-links a {
    color: var(--color-text-light);
    text-decoration: none;
    transition: all var(--transition-normal);
    padding: var(--space-xs) 0;
    border-radius: var(--radius-sm);
  }
  
  .social-links a:hover {
    color: var(--color-accent);
    text-decoration: underline;
    transform: translateX(4px);
  }
  
  .footer-bottom {
    border-top: 1px solid var(--color-border);
    padding-top: var(--space-lg);
    text-align: center;
    margin-top: var(--space-xl);
  }
  
  .footer-bottom p {
    color: var(--color-text-muted);
    margin-bottom: var(--space-sm);
  }
  
  .footer-tech {
    font-size: 0.875rem;
    color: var(--color-text-muted);
  }
  
  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
      text-align: center;
    }
  }
</style>